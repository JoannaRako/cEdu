#include <stdio.h>
#include <math.h>

int n, i; 
long int sum;

int main(void) {

printf("Wpisz liczbe (>1)\n");
scanf("%d", &n);

sum = 0;

for(i = 1; i <= n; i++){
  sum = sum + pow(i, 2);
  printf("%d: %li\n", i, sum);
} 
printf("Suma całkowita %li\n", sum);
return 0;
}