#include <stdio.h>
#include <math.h>

int main(void){

double v, pp;
double r;
double pi = 3.14159;

printf("Podaj promien: ");

scanf("%lf", &r);
    
v = (4./3.)*pi*pow(r, 3);
pp = 4.*pi*pow(r, 2);
    
printf("Objetosc: %lf\n", v);
printf("Pole powierzchni: %lf\n", pp);

return 0;
}